VITE_PROXY_URL=
VITE_GOOGLE_CLIENT_ID=
# Set to "true" to build a self-contained demo instance (auto-seeds demo data,
# skips onboarding and all bank/sync/backup flows). Leave unset for normal builds.
VITE_DEMO_ONLY=
